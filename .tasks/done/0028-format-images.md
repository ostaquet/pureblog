# Problem to solve

The size of the images doesn't work well in the layout.

If the image is bigger than the width of the text, it should be shrink to the width of the text at the rendering.

If the image is smaller than the width of the text, we do nothing.

If the user resize the window, the image should be shrink to the width of the text at the rendering.
