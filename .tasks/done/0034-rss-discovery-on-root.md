# Problem to solve

The indexes of a language (under the URL : <root>/<lang>) has a link to the RSS discovery (`<link rel="alternate" type="application/rss+xml" ...>`).

The root index which is a reload to the default language doesn't have this RSS discovery link. It should be added to the default language RSS feed.
