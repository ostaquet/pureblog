# Problem to solve

In `src/build.py` in the function `render_lang_switcher()`, there is a CSS class defined as `missing-translation` and the `<s>` HTML tag.

I would prefer to use only the CSS class and to define the strikethrough in the `theme/style.css` to ease the customization by the user.
