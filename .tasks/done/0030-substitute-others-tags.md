# Problem to solve

The `theme/template.html` contains two elements that should be configured through the `config/config.yml`.

The header h1 `Olivier's Blog` should be replaced by the `general.site_title`.

The footer p `Olivier` should be replaced by the `general.author` and we should also add the current year. (if author is `Olivier`, I'm expecting a footer `© Olivier 2026`).
