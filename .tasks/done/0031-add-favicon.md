# Problem to solve

Modern web browser tries to download `favicon.ico` as an icon for the tab.

As Pureblog is a very simple blog engine, I would like to add a favicon under the form of a emoji. The favicon is defined in the `config/config.yml` with a parameter `general.favicon_emoji`. The system must check if the emoji is well a single character as emoji. This emoji should be use as favicon.ico.
