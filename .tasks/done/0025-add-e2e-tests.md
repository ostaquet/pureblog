# Problem to solve

The unit tests are good but I would like to ensure that the process is running correctly end to end.

I'm expecting that we run the builder program in a Docker container to create a test blog, build the static website with the builder, serve it in HTTP and run playwright to open the webpage.
