# Problem to solve

The `src/build.py` has been refactored to separate the site builder (now in `src/builder.py`) and the arg parsing (in `src/main.py`).

The unit tests must be refactored too and the documentation needs to be updated.
