# Problem to solve

The source code is kept at the root of the project. It is a bit messy. The source code should be in the `src` folder.

All related source files should be in the `src` folder, it includes the main `build.py` but also the CSS, HTML and test files.
