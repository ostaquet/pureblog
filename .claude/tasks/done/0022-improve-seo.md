# Problem to solve

Improve SEO by adding `sitemap.xml` autogenerated based on the existing page and add `robots.txt` to define where is the sitemap. `robots.txt` is a static file that should be defined in the `seo/robots.txt`. When the sitemap is generated, we only ensure that the sitemap location is properly set in the `robots.txt` file with the `Sitemap:` directive.
