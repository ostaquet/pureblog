# Problem to solve

When reading the code, there are some flaws. I'm expecting some basic rules to follow to keep a sane code base:

- The typing must be explicit (variables, parameters of functions, return values of functions).
- I'm using Pylance as a linter. When Pylance is checking the code, I'm expecting that it passes without warnings. The ignore statements are not allowed.
- Function names should always be verbs.
- Variable names should always be nouns.
- Don't write clever code, write understandable code.
- Keep the functions short, one screen maximum to be readable by a human (maximum 60 lines of code).

Ensure that these rules are enforced and adjust the code if needed.
Keep the rules in mind for the future.
